# Versions below are the combination VERIFIED working on Windows/Python 3.12 (2026-08-25).
# Install:  pip install -r requirements.txt  &&  pip install -e .

# Core
numpy
pandas
pyarrow            # parquet tables (utils.io falls back to csv if missing)
pyyaml

# Ocean data
xarray
netCDF4
scipy
copernicusmarine   # GLORYS12 download (needs a free CMEMS account) — Unit B
argopy             # Argo float profiles — Unit B/C
erddapy<3          # PIN: argopy 1.4.0 imports a private symbol removed in erddapy 3.x
certifi            # CA bundle; download_argo sets SSL_CERT_FILE from it (Windows SSL fix)

# ML
scikit-learn
lightgbm           # baseline + quantile uncertainty — Unit A
torch              # per-column MLP — Unit A

# App / viz
streamlit          # demo UI — Unit B shell + Unit C panels
plotly
matplotlib

# Dev
pytest

# Export & API (Prompt 7). fastapi is pinned loosely but starlette is NOT pinned here on purpose:
# streamlit requires starlette<2,>=0.46.0 and uvicorn<1,>=0.30.0, and Streamlit is the demo. Let
# pip resolve starlette for streamlit and check that fastapi accepts it -- never the reverse.
# Verified 2026-09-05: installing fastapi left starlette at 1.6.0 and all ten pages still load.
fastapi
uvicorn
