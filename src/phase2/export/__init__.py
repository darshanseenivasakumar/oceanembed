"""Product files that leave the building. (Unit A / Arjhun.)

Everything here writes something a person outside this repo will open, so the rule is stricter than
elsewhere: a file must carry enough provenance to say what produced it, and an absence must read as
an absence. See `netcdf.py`.
"""
