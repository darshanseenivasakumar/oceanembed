# --- Data & large files (NEVER commit these) ---
data/
data/raw/
data/processed/
*.nc
*.zarr/
*.grib

# Research papers stay local, never in the repo
all research papers/
../all research papers/

# --- Model artifacts: ignore everything EXCEPT the tiny fixtures ---
artifacts/*
!artifacts/sample_X.npy
!artifacts/sample_y.npy
!artifacts/sample_meta.parquet
*.pt
*.pkl
*.ckpt

# --- Secrets / credentials (NEVER commit) ---
.env
.env.*
*.key
*_token*
credentials*
.copernicusmarine/

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
env/
.ipynb_checkpoints/
.pytest_cache/

# --- OS / editor junk ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# --- Local memory (keep private) ---
.remember/
