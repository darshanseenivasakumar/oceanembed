# UNIT B — DARSHAN — Backbone & Integrator

> **You move FIRST.** Arjhun and Mitun+Niru are blocked until your Day-1 scaffold + fixtures are pushed.
> You also **integrate LAST** (wire everyone's pieces into the app on Day 5).
> Account: Claude Pro. Read `SHARED_BRIEF.md` first, then this file.

## Your charter
The spine of the project: the whole **data pipeline** (download + preprocess + build training tensors), the single
**`reconstruct()` API** that ties everyone's work together, and the **Streamlit app shell**. You are the "pipeline +
hard stuff + integration" person.

## Files you OWN (only you edit these)
```
git repo scaffold, README.md, requirements.txt, .gitignore
config/config.yaml
docs/DATA_CONTRACT.md   docs/MASTER_SPEC.md   docs/DEMO_SPEC.md   CLAUDE.md
src/oceanembed/utils/io.py        src/oceanembed/utils/grids.py
src/oceanembed/data/download_glorys.py   download_argo.py   preprocess.py
src/oceanembed/features/build_samples.py
src/oceanembed/inference/predict.py      # THE integration seam
app/streamlit_app.py                     # the shell (imports C's panels)
tests/test_shapes.py                     # the guardian test
```
**Do NOT touch:** anything in `models/`, `train/`, `inference/uncertainty.py`, `products/`, `climatology.py`,
`validation/`, `app/panels/` — those belong to A and C.

## What you produce for others (into `artifacts/`)
`sample_*` fixtures (Day 1), then the real `X_train/X_test/y_train/y_test .npy`, `meta_*.parquet`, `norm_stats.json`,
`land_mask.npy`, `argo_test.parquet`. Everyone else reads these. Schemas are in `SHARED_BRIEF.md`.

## Your phases

### Day 1 (Aug 25) — Scaffold + contracts + fixtures  ← unblocks the whole team
1. `git init`, create the full folder tree, empty owned files, push to GitHub (`main`).
2. Write `config/config.yaml` with the frozen constants (LAT, LON, DEPTHS, FEATURES, paths, split years).
3. Write `CLAUDE.md` (the constitution — copy the rules from `SHARED_BRIEF.md`) and `docs/DATA_CONTRACT.md`
   (dataset + variable + file-schema tables) and a short `docs/MASTER_SPEC.md`.
4. Write `.gitignore` (ignore `artifacts/*` except fixtures, `data/`, `.env`, secrets, `__pycache__`).
5. Write `src/oceanembed/utils/grids.py`: helpers for LAT/LON/DEPTHS + `latlon_to_cell_id(lat,lon)`.
6. **Generate fixtures:** a script that writes `artifacts/sample_X.npy (500,11)`, `sample_y.npy (500,11)`,
   `sample_meta.parquet` with correct columns — random values, correct shapes/dtypes. Commit these.
7. **Acceptance:** teammates can `python -c "import numpy; numpy.load('artifacts/sample_X.npy').shape"` → `(500, 11)`.

### Day 2 (Aug 26) — Download + preprocess (real data)
1. `download_glorys.py` — use the `copernicusmarine` toolbox (free CMEMS account). Subset NIO bbox, variables
   `thetao, so, zos, uo, vo`, years 2019–2022. **If download/RAM is heavy, subsample to weekly.** Save to `data/raw/`.
   → Use Context7 for the current `copernicusmarine subset` command.
2. `download_argo.py` — use `argopy` to fetch 2022 NIO profiles. Save to `data/raw/`.
3. `preprocess.py` — regrid everything to 0.25° on the frozen LAT/LON grid, build land mask, compute day-of-year
   features, z-score using **train years only** → write `norm_stats.json` and `land_mask.npy`.
   **VERIFY by inspection** before trusting: print lat/lon order, depth sign, variable units, NaN pattern. Record
   findings in `DATA_CONTRACT.md` with a `[VERIFIED]` tag.

### Day 3 (Aug 27) — Build training tensors
1. `features/build_samples.py` — flatten (grid-cell, day) into rows → write `X_train/X_test/y_train/y_test.npy`,
   `meta_train/meta_test.parquet`, `argo_test.parquet`. Drop land/NaN cells.
2. `tests/test_shapes.py` — assert every `artifacts/` file matches the contract shapes/dtypes. This is the guardian.
3. **Real files now replace fixtures with identical shapes → A and C need zero code changes.** Tell the team to pull.

### Day 4 (Aug 28) — Integration seam
1. `inference/predict.py` — implement `reconstruct(lat, lon, date)` and `reconstruct_grid(date)` that call A's model +
   A's `mc_dropout_predict` + C's `climatology_predict`/`anomaly` + A's `observation_priority`, and return the frozen
   output dict (see `docs/MODEL_SPEC.md` — Arjhun writes that signature; you consume it).
2. Start `app/streamlit_app.py` shell: sidebar (lat/lon/date pickers), and mount points that call C's panel `render()`.

### Day 5 (Aug 29) — Integrate + rehearse
1. Pull A's and C's branches, wire panels into the shell.
2. Write `docs/DEMO_SPEC.md`: 2–3 demo scenes (date + coords + expected output), and cache their tensors **generated by
   the real model**. Clearly label LIVE vs CACHED in the UI.
3. Run the full judge click-path **twice** offline. Fix breakage. Confirm Definition of Done is green everywhere.

## Copy-paste block for your Claude (after SHARED_BRIEF)
```
I am UNIT B (Darshan). I own: repo scaffold, README, requirements.txt, .gitignore, config/config.yaml,
docs/DATA_CONTRACT.md, docs/MASTER_SPEC.md, docs/DEMO_SPEC.md, CLAUDE.md, src/oceanembed/utils/*,
src/oceanembed/data/*, src/oceanembed/features/build_samples.py, src/oceanembed/inference/predict.py,
app/streamlit_app.py, tests/test_shapes.py. I must NOT touch models/, train/, inference/uncertainty.py, products/,
climatology.py, validation/, app/panels/. Today is Day 1: create the repo tree + contracts + fixtures FIRST so my
teammates can start, then push. Then GLORYS/Argo download, preprocess, build_samples producing all shared artifacts to
the DATA_CONTRACT schema, then the reconstruct() seam and the Streamlit shell. I integrate everyone last.
```
